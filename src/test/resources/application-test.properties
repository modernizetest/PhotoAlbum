# Test configuration - use H2 in-memory database in PostgreSQL compatibility mode
# Migrated from Oracle to PostgreSQL according to Java check item 26: Change H2 to PostgreSQL mode.
spring.datasource.url=jdbc:h2:mem:testdb;MODE=PostgreSQL;DB_CLOSE_DELAY=-1
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=

# JPA Configuration for testing
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=false

# File upload configuration for testing
app.file-upload.upload-path=target/test-uploads
app.file-upload.max-file-size-bytes=10485760
app.file-upload.allowed-mime-types=image/jpeg,image/png,image/gif,image/webp
app.file-upload.max-files-per-upload=10

# Logging for tests
logging.level.com.photoalbum=DEBUG